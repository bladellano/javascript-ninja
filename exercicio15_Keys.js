var obj = {
	x:1,
	y:2,
	z:3
}

console.log("obj", obj);

Object.keys(obj);
console.log("Array de Chaves: ", Object.keys(obj));

Object.keys(obj).length;
console.log("Qtd: ", Object.keys(obj).length);
